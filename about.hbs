Third-party notices for shr-rs
==============================================================================

shr-rs itself is dual-licensed MIT OR Apache-2.0; see LICENSE-MIT and
LICENSE-APACHE. The `shr-rs` binary is statically linked, so every crate below
is compiled into the shipped executable and redistributed with it. Each is
listed with the terms it is redistributed under.

Generated by `cargo about` from the Cargo.lock of this release, restricted to
the x86_64-unknown-linux-musl target that is actually built.

Summary
-------
{{#each overview}}
{{name}} ({{count}} {{#if (eq count 1)}}crate{{else}}crates{{/if}})
{{/each}}

{{#each licenses}}
==============================================================================
{{name}}

Used by:
{{#each used_by}}
  - {{crate.name}} {{crate.version}}{{#if crate.repository}} ({{{crate.repository}}}){{/if}}
{{/each}}
==============================================================================

{{{text}}}

{{/each}}
