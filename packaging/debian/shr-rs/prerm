#!/bin/sh
set -e

# Only on removal, not on upgrade: an upgrade leaves a running tailer alone.
if [ "$1" = remove ] && [ -d /run/systemd/system ]; then
    systemctl --system stop shr-rs.service >/dev/null 2>&1 || true
fi
